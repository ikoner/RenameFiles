# RenameFiles
batch rename files

RenameFiles path prefix|postfix
path is optional parameter.
Example:
d:/temp

prefix|postfix is mandatory parameter.
Examples:
prefix|postfix
prefix - no postfix
|postfix - no prefix
